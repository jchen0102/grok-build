# 2026-09-12 产品锁定（十五轮）

> **对话详情不在本文件。** 打开同目录 [`完整对话.md`](./完整对话.md)。本表只记锁定结论。

时区：UTC。用户每轮从选项里拍板，未选的不写进实现。

**钟点：** 变更记录只写了「2026-09-12：十五轮全部锁定」。辩论没有按轮 commit，**十五轮都标这一天，没有编造每轮的 HH:MM**。代码落地的分钟见 `2026-09-12-竖切实现.md`（10:36:53Z–10:54:39Z）。

| 轮 | 日期 | 题 | 用户锁定 |
|---|---|---|---|
| 1 | 2026-09-12 | 为谁做、要不要登录 | 平台 IC + lead；无登录；默认总览；空库停看板壳 + CTA |
| 2 | 2026-09-12 | 6–8h 完成标准 | 导入 demo → 六指标+趋势 → 点异常数进聚合 → 原始记录可筛可翻页；同一时间窗 |
| 3 | 2026-09-12 | 必做 / 选做 | 必做五项全做；选做全砍；趋势只按小时；一份静态 `data/demo.csv`；搜索/排序/分页仍做 |
| 4 | 2026-09-12 | 计算放哪 | Postgres 滤行，Go 算指标；缝 = `internal/metrics`；×1000 只写 README |
| 5 | 2026-09-12 | 空态 / 导航 | 廉价 loading/empty/error；顶栏四页；异常数下钻聚合 + 同窗 + `anomalous=1`；导入页不读时间窗 |
| 6 | 2026-09-12 | 导入语义 | 同步；流式+批量；成功/重复/失败；分类明细+样本封顶 5；空 `request_id`=失败不入库；先到先得；只 trim |
| 7 | 2026-09-12 | 时间 / p95 | `timestamptz` UTC；默认 min–max；闭区间；小时桶 `[h,h+1)`；p95 = nearest-rank `ceil(0.95*n)` 1-based |
| 8 | 2026-09-12 | 异常 / 5xx | n≥20 且（5xx 率≥10% 或 p95≥1000ms）；双原因都展示；4xx 不算错误率；样本=全部入库状态码 |
| 9 | 2026-09-12 | 表 | 单表 `api_requests`；PK=`request_id`；六列 NOT NULL；只要 `INDEX(ts)` |
| 10 | 2026-09-12 | HTTP | `/api/imports` `/api/overview` `/api/endpoints` `/api/requests`；部分成功 200；缺文件/缺表头 400；`{error:{code,message}}`；page 默认 20、上限 100 |
| 11 | 2026-09-12 | 测什么 | 只测 p95 + 异常；表驱动；不测导入、不写 httptest、不做前端 E2E |
| 12 | 2026-09-12 | 提交形态 | `docker compose up --build`；`data/demo.csv`；`ai-log/` + README 指针；README 写导入/去重/p95/时区/性能/×1000 |
| 13 | 2026-09-12 | 顺序 | 竖切：表 → 导入 → 指标+测 → 总览 → 聚合 → 原始 → compose/README/ai-log |
| 14 | 2026-09-12 | 趋势 / 排序 | 默认请求量+5xx 次数，可切 5xx 率；空桶次数 0、比率空；异常优先再按量；子串搜索；每页 20 |
| 15 | 2026-09-12 | 主键 / 列表序 | PK=`request_id`；`ORDER BY ts DESC, request_id DESC` + OFFSET；状态码 100–599 |

同日栈锁（实现按此，不再换）：**chi + pgx + golang-migrate + Vite + Recharts**。
