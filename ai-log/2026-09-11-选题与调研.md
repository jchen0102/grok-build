# 2026-09-11 选题与调研

> **对话详情不在本文件。** 打开同目录 [`完整对话.md`](./完整对话.md)。

时区：UTC。

**钟点：** 调研文写检索日 `2026-09-11`，当日仓库没有本题 commit，**只有日期，没有可靠的 HH:MM**，不补造。

## 2026-09-11

用户要在 48h take-home（建议投入 6～8h）里选题。栈固定 **React + Go + PostgreSQL**，必须用 AI 并交对话记录。题库里先对比 C / D，再做一道。

**拍板：做 C（API 调用质量监控台），不做 D（设备预约）。**

依据当时对比：

- C 考批量导入是否正确、指标口径是否自洽、查询是否按数据量设计。
- D 考互斥占用、并发只成功一个、角色和状态机。
- C 和 MaaS 网关同构（service / endpoint / method / status / latency），没有角色矩阵。

## 2026-09-11 同日 · 调研（只抄形状）

检索日写在调研文里：2026-09-11。

用户要求：商业竞品、开源可观测、网关分析、GitHub 相近项目都先落盘，**只借结构，不借实现**。

抄：

- RED 总分：卡片 → 小时趋势 → 接口表 → 原始行
- 一个顶栏时间窗
- 均延迟和 p95 并排
- 5xx ≠ 4xx
- 导入后看批量 Insights

不抄：OTLP、Prometheus、Grafana、告警、Watchdog / ML、登录、实时探针、可配阈值。
